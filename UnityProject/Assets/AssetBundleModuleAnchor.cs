using UnityEngine;

// Build-project-only anchor: keeps Unity native AssetBundle classes in the
// Standalone Windows type database for this shader-only project.
internal sealed class AssetBundleModuleAnchor : MonoBehaviour
{
    public AssetBundle Bundle;
    public AssetBundleManifest Manifest;
}
